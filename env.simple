# Configuration Astro Alignement - Fichier unique pour local ET production
# L'application détecte automatiquement l'environnement et utilise les bonnes valeurs

# ========================================
# CONFIGURATION OBLIGATOIRE
# ========================================

# Clé API OpenAI (remplacez par votre vraie clé)
VITE_OPENAI_API_KEY=sk-your_openai_api_key_here

# URL de votre backend
VITE_API_BASE_URL=http://localhost:3000/api

# ========================================
# CONFIGURATION OPTIONNELLE
# ========================================

# Port du serveur local
VITE_SERVER_PORT=8080

# Modèle OpenAI
VITE_OPENAI_MODEL=gpt-4

# Timeout API (en millisecondes)
VITE_API_TIMEOUT=10000

# Clé API astrologie (optionnel)
VITE_ASTROLOGY_API_KEY=your_astrology_api_key_here

# URL API astrologie
VITE_ASTROLOGY_API_URL=https://api.astrologie.com

# Base de données (optionnel)
VITE_DATABASE_URL=postgresql://user:password@localhost:5432/astro_db

# Google Analytics (optionnel)
VITE_GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID

# Sentry pour le monitoring (optionnel)
VITE_SENTRY_DSN=your_sentry_dsn_here

# Secrets de sécurité (optionnel en local, obligatoire en production)
VITE_JWT_SECRET=dev_jwt_secret_change_in_production
VITE_SESSION_SECRET=dev_session_secret_change_in_production

# Version de l'application
VITE_APP_VERSION=1.0.0

# ========================================
# COMMENT ÇA MARCHE
# ========================================
#
# 🚀 EN LOCAL :
# - L'application utilise les valeurs de ce fichier .env
# - VITE_API_BASE_URL pointe vers localhost:3000
# - Mode mock activé automatiquement
#
# 🌐 SUR RENDER :
# - Dans "Environment Variables", ajoutez EXACTEMENT les mêmes noms
# - VITE_API_BASE_URL = https://votre-backend.onrender.com/api
# - L'application détecte automatiquement qu'elle est sur Render
# - Mode production activé automatiquement
#
# ✅ BASCULEMENT AUTOMATIQUE :
# - Local → Mode développement avec données mock
# - Render → Mode production avec API réelle
# - Aucune modification de code nécessaire !
